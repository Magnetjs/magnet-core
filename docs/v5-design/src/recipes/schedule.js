const modules = [
  require("@magnetjs/bull").default,
  require("@magnetjs/config").default,
  require("@magnetjs/folder-loader").default,
  require("@magnetjs/pino").default,
  require("@magnetjs/node-schedule").default
];

export default modules;
